-- Mengecek key dari getgenv()
if getgenv().Key == "X5SpiderWin" then
    print("Key is valid, executing script...")
    
    -- Memuat dan mengeksekusi skrip dari URL yang diberikan
    local success, err = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Rndm373/All-script/refs/heads/main/spider.txt"))()
    end)

    if not success then
        -- Jika ada kesalahan dalam eksekusi, tampilkan pesan error
        warn("Error loading the script: " .. err)
    end
else
    print("Invalid key! Script will not execute.")
end



